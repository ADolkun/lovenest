.env
secrets/
.worktrees/
.DS_Store
.playwright-mcp/
.claude
.mcp.json
docs/plans/
tmp/
docker-compose.override.yml

# Ignore local configuration files
.vscode/

# Runtime artifacts written by the agents feature (uploaded KB files,
# downloaded ONNX embedding models, transaction attachments). User data
# — never commit.
backend/data/

# Household hub — local secrets & personal data (never commit)
.hub-credentials.json
HANDOFF.md
CONTINUE-HERE.md
OVERVIEW.html
backups/
# Personal tooling & local state — live SimpleFIN token, real emails, workspace ids.
# Only the upstream-tracked dev helper stays published.
scripts/
!scripts/dev-frontend.mjs

# Vestigial bind-mount overlays (the fixes are baked into source on this branch)
# and personal sample data.
patches/
holdings-template.csv
